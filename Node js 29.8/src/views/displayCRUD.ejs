<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Get CRUD</title>
    <style>
        table,
        th,
        td {
            border: 1px solid black;
            border-collapse: collapse;
            padding: 8px;
            text-align: left;
        }

        .btn-edit {
            background: greenyellow;
            outline: none;
            border: none;
            height: 25px;
            width: 60px;
            border-radius: 3px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            text-align: center;
            line-height: 25px;
            color: black;
        }

        .btn-delete {
            background: red;
            outline: none;
            border: none;
            height: 25px;
            width: 60px;
            border-radius: 3px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            text-align: center;
            line-height: 25px;
            color: white;
        }
    </style>
</head>

<body>
    <h2>Danh sách Users</h2>
    <table style="width:100%">
        <tr>
            <th>Email</th>
            <th>Firstname</th>
            <th>Lastname</th>
            <th>Actions</th>
        </tr>

        <% if (dataTable && dataTable.length> 0) { %>
            <% dataTable.forEach((user)=> { %>
                <tr>
                    <td>
                        <%= user.email %>
                    </td>
                    <td>
                        <%= user.firstName %>
                    </td>
                    <td>
                        <%= user.lastName %>
                    </td>
                    <td>
                        <a href="/edit-crud?id=<%= user.id %>" class="btn-edit">Edit</a>
                        <a href="/delete-crud?id=<%= user.id %>" class="btn-delete">Delete</a>

                    </td>
                </tr>
                <% }) %>
                    <% } else { %>
                        <tr>
                            <td colspan="4">No users found</td>
                        </tr>
                        <% } %>
    </table>
</body>

</html>